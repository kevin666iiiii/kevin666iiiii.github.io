"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getProvinceByCoordinates = getProvinceByCoordinates;
exports.getProvince = getProvince;
const constants_1 = require("./constants");
async function getProvinceByCoordinates(longitude, latitude) {
    const url = `https://restapi.amap.com/v3/geocode/regeo?key=${constants_1.GD}&location=${longitude},${latitude}`;
    try {
        const response = await fetch(url);
        const data = await response.json();
        if (data.status === '1' && data.regeocode) {
            return data;
        }
        return '未知';
    }
    catch (error) {
        console.error('获取地理位置失败:', error);
        return '未知';
    }
}
async function getProvince(longitude, latitude) {
    const url = `https://restapi.amap.com/v3/geocode/regeo?key=${constants_1.GD}&location=${longitude},${latitude}`;
    try {
        const response = await fetch(url);
        const data = await response.json();
        if (data.status === '1' && data.regeocode) {
            return data.regeocode.addressComponent.province;
        }
        return '未知';
    }
    catch (error) {
        console.error('获取地理位置失败:', error);
        return '未知';
    }
}
//# sourceMappingURL=geo.js.map
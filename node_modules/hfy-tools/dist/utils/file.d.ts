/**
 * 打开文件选择对话框
 * 若浏览器不支持实验性方法：window.showOpenFilePicker，则采用input[type=file]元素进行兼容
 * @param {string | string[]} accept 文件类型限制 ，默认全部
 * @param {boolean} multiple 文件多选
 * @param {boolean} forceInput 强制input
 * @param {boolean} webkitdirectory 只选择目录限制
 * @param {number} compatible 兼容模式，默认开启
 * @param {number} cancel 兼容取消控制，为0时候则取消文件时不抛出reject，❗在使用async/await时会造成阻塞
 * @param {string} description 文件或者文件夹的描述，可选
 * @return {Promise<FileList>}
 */
export interface IChooseFileOptions {
    forceInput?: boolean;
    accept?: string | [];
    cancel?: number;
    compatible?: boolean;
    multiple?: boolean;
    webkitdirectory?: boolean;
    description?: string;
}
export declare function overwrite(content: string, options: IChooseFileOptions): Promise<void>;
export declare function chooseFile(options: IChooseFileOptions): Promise<File[]>;
export declare function downloadFile(blob: File, downloadName: string): void;
export declare const FILE_TYPES: Readonly<{
    AUDIO: "audio";
    VIDEO: "video";
    OFFICE: "office";
    IMAGE: "image";
    MODEL: "model";
    CAD: "cad";
    ARCHIVE: "archive";
    UNKNOWN: "unknown";
}>;
export declare function getFileType(fileName: any): "audio" | "video" | "image" | "office" | "model" | "cad" | "archive" | "unknown";
export declare function urlToBlob(url: string): Promise<Blob>;

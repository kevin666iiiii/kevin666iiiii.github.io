import Text from '../resouce/sensi_words';

export function filterText(content: string, forContent) {
    let result = '';
    Text.split('\n').forEach(text => (result = content.replace(text, forContent)))
    return result;
}


export interface IResult<D, E = Error> {
    code: number;
    msg: string;
    data?: D;
    error?: E;
    success: boolean;
}
export declare function toResult<T, E>(data: T, code: number, msg: any, error: E): IResult<T, E>;

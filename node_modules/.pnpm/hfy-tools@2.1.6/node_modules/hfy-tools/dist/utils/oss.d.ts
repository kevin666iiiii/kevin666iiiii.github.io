import { DeleteMultipleObjectResult, GetObjectResult, GetObjectUrlResult, PutObjectCopyResult, SliceUploadFileResult } from 'cos-js-sdk-v5';
export interface IFileJSON {
    key: string;
    dir: string;
    name: string;
    isDir?: string;
}
declare class OSS {
    Bucket: string;
    Region: string;
    SecretKey: string;
    SecretId: string;
    prefix: string;
    isTemp: boolean;
    cos: any;
    params: {
        Bucket: string;
        Region: string;
    };
    constructor({ Bucket, Region, SecretKey, SecretId, prefix, isTemp }: {
        Bucket: string;
        Region: string;
        SecretKey: string;
        SecretId: string;
        prefix?: string;
        isTemp?: boolean;
    });
    /**
     *
     * @param file 文件
     * @param key 上传文件key
     * @param progress 上传进度函数回调
     * @param dir 目录
     * @returns
     */
    upload({ file, dir, progress, key, expirationDate }: {
        file: File;
        dir?: string;
        progress?: Function;
        key?: string;
        expirationDate?: Date;
    }): Promise<SliceUploadFileResult>;
    /**
     * 批量删除文件
     * @param keys 文件key
     * @returns
     */
    multiDelete(...keys: string[]): Promise<DeleteMultipleObjectResult>;
    /**
     * 获取文件
     * @param key
     * @param progress
     * @param isGetUrl
     * @returns
     */
    getFile(key: string, progress: Function, isGetUrl?: boolean): Promise<GetObjectUrlResult | GetObjectResult>;
    /**
     * 文件拷贝
     * @param key
     * @param sourceKey
     * @param pms
     * @returns
     */
    copy(key: string, sourceKey: string, pms?: {}): Promise<PutObjectCopyResult>;
    /**
     * 根据前缀获取文件
     * @param pre
     * @returns
     */
    getFiles<T extends IFileJSON = IFileJSON>(): Promise<T[]>;
    getDirectory(): Promise<any>;
    buildDirectory(files: any[]): any;
}
declare class DB {
    OSS: OSS;
    constructor(ossParams: {
        Bucket: string;
        Region: string;
        SecretKey: string;
        SecretId: string;
        prefix?: string;
        isTemp?: boolean;
    });
    add<T>(name: string, data: any): Promise<T>;
    update(name: string, id: string, data: any): Promise<unknown>;
    delete(name: string, id: string): Promise<unknown>;
    get<T>(name: string): Promise<T>;
    list(): Promise<unknown>;
}
declare const _default: {
    COS: typeof OSS;
    DB: typeof DB;
};
export default _default;
